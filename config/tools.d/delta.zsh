# ============================================================================
# Delta — options
# ============================================================================

# Delta config is in git/.gitconfig [delta] section
# No shell aliases needed — delta is used as GIT_PAGER
