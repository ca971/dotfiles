# ============================================================================
# Topgrade aliases
# Loaded by tools/topgrade.zsh (only if topgrade is installed)
# ============================================================================

alias upgrade="topgrade"
alias upgrade-yes="topgrade --yes"
alias upgrade-dry="topgrade --dry-run"
alias upgrade-clean="topgrade --cleanup"
