# ============================================================================
# Hyperfine — CLI benchmarking
# ============================================================================

alias bench="hyperfine"
alias bench-shell="hyperfine --warmup 3 'zsh -ic exit' 'bash -ic exit'"
