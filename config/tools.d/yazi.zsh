# ============================================================================
# Yazi — options
# ============================================================================

export YAZI_CONFIG_HOME="${XDG_CONFIG_HOME:-${HOME}/.config}/yazi"
export YAZI_FILE_EDITOR="${EDITOR:-nvim}"
