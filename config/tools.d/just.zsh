# ============================================================================
# Just — aliases
# ============================================================================

alias j="just"
alias jl="just --list"
alias jlu="just --list --unsorted"
alias js="just --show"
alias jd="just --dry-run"
alias je="just --evaluate"
