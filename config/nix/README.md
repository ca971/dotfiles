# ❄️ Nix Configuration

Declarative, cross-platform environment management.

## Quick Start

```bash
# Enter dev shell (all tools available)
cd ~/dotfiles/config/nix
nix develop

# Install all packages to your profile
nix profile install .

# Update all inputs
nix flake update
```
